<nav id="mainNav" class="navbar navbar-default navbar-custom navbar-fixed-top ltt-navbar">
  <div class="container">
    <div class="header-left navbar-header page-scroll col-md-9">
      <ul class="nav navbar-nav">
        <li id="title" class="navbar-brand page-scroll">
          <%= link_to t(:title), root_url, class: "navbar-brand page-scroll ltt-title" %>
        </li>
        <li class="header-search">
          <%= form_tag searches_path, method: :get do %>
            <span>
              <%= text_field_tag :search, params[:search],
                placeholder: t("search.title"), class: "form-control
                images-search", id: "images-search" %>
              <i class="fa fa-search" aria-hidden="true"></i>
            </span>
          <% end %>
          <span class="search-results">
            <ul class="results">
            </ul>
          </span>
        </li>
      </ul>
    </div>
    <span class="header-right">
      <li class="hidden">
        <a href="#page-top"></a>
      </li>

      <li class="dropdown header-item">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"
          title="<%= current_user.name %>">
          <%= image_tag avatar_profile(current_user), class: "avatar-header" %>
          <span class="header-user-name">
            <%= truncate current_user.name, length: Settings.length_name %>
          </span>
          <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li>
            <%= link_to t("sessions.profile"), current_user %>
          </li>
          <li>
            <%= link_to t("sessions.image_bookmark"), user_book_marks_path(current_user) %>
          </li>
          <li>
            <%= link_to t("sessions.sign_out"), destroy_user_session_path, method: :delete %>
          </li>
        </ul>
      </li>
      <div class="clearfix"></div>
    </span>
  </div>
</nav>
